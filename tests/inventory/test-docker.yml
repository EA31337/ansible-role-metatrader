---
all:
  children:
    docker_containers:
      hosts:
        metatrader-on-package-image:
          ansible_connection: docker
          ansible_host: metatrader-on-package-image
          ansible_python_interpreter: /usr/bin/python3
