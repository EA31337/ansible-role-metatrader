---
all:
  children:
    docker_containers:
      hosts:
        metatrader-on-wine-latest:
          ansible_connection: docker
          ansible_host: metatrader-on-wine-latest
          ansible_python_interpreter: /usr/bin/python3
