---
# yamllint disable rule:line-length
metatrader_setup_url: https://download.mql5.com/cdn/web/metaquotes.software.corp/mt5/mt5setup.exe
metatrader_version: 5

metatrader_wine_release_codename: "{{ ansible_facts.distribution_release | lower }}"
metatrader_wine_version: "{{ '9' if (ansible_facts.os_family | default('Debian')) == 'Alpine' else '10.0' }}"
